[Unit]
Description=Windows bridge for GPG Agent's ssh socket
Requires=gpg-agent-ssh.socket
CollectMode=inactive-or-failed

[Service]
# EnvironmentFile=%E/gpg-bridge/git-for-windows.env
# EnvironmentFile=%E/gpg-bridge/gnupg-standalone.env
# ExecSearchPath=
ExecStart=gpg-bridge.exe agent-ssh-socket
StandardInput=socket
StandardOutput=socket
StandardError=journal
